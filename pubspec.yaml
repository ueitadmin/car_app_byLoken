name: car_km_tracker_app
description: A simple app to track car kilometers using GPS
publish_to: "none"

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.0
  location: ^5.0.3

flutter:
  uses-material-design: true
